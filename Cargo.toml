[package]
name = "crystal-savant"
version = "0.1.0"
edition = "2021"
description = "Programming Savant: Crystal compression + BTR + RL pipeline for massive context awareness"

[dependencies]
rand = "0.8"
rayon = "1.8"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
